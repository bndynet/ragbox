# Private Roadmap Draft

This draft is intentionally excluded by `examples/ragbox.config.json`.

It exists to verify that the `web` source can include Markdown and MDX files while excluding draft content with the pattern `**/draft/**`.
